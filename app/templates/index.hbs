
<h1 class="logo">{{fa-icon "arrow-circle-down"}}</h1>
<div class="login-container">
  <div>{{input class="login-inputs" placeholder='Username'}}</div>
  <div>{{input class="login-inputs" placeholder='Password'}}</div>
  <div>
    {{#link-to 'home'}}
      <button class="login-button" name="login-button">Login</button>
    {{/link-to}}
  </div>
</div>

{{outlet}}
